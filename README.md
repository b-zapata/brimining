# BRI Mining

React web app for the BRI Mining website.

## Development

Install dependencies:

```sh
pnpm install
```

Start the local dev server:

```sh
pnpm dev
```

Build for production:

```sh
pnpm build
```
